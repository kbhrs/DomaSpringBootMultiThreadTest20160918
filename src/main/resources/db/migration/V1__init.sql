CREATE TABLE country (
  country_name varchar(200) NOT NULL,
  PRIMARY KEY (country_name)
);

insert country values('JAPAN');
insert country values('UK');
insert country values('US');
insert country values('BRAZIL');
